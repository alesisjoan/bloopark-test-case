<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="background_tasks.list">
      <field name="name">background_tasks list</field>
      <field name="model">background_tasks.background_tasks</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="background_tasks.action_window">
      <field name="name">background_tasks window</field>
      <field name="res_model">background_tasks.background_tasks</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="background_tasks.action_server">
      <field name="name">background_tasks server</field>
      <field name="model_id" ref="model_background_tasks_background_tasks"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="background_tasks" id="background_tasks.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="background_tasks.menu_1" parent="background_tasks.menu_root"/>
    <menuitem name="Menu 2" id="background_tasks.menu_2" parent="background_tasks.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="background_tasks.menu_1_list" parent="background_tasks.menu_1"
              action="background_tasks.action_window"/>
    <menuitem name="Server to list" id="background_tasks" parent="background_tasks.menu_2"
              action="background_tasks.action_server"/>
    -->
  </data>
</odoo>